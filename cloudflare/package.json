{
  "name": "cb-butchers-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply cb-butchers-calendar --local",
    "db:migrate:remote": "wrangler d1 migrations apply cb-butchers-calendar --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
